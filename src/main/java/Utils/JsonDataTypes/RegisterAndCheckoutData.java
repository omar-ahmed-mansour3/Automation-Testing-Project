package Utils.JsonDataTypes;

public class RegisterAndCheckoutData {
    // These names MUST match the keys in the JSON file
    public RegisterData user;
    public BankData card;

    // You don't need to rewrite the fields for name, email, etc.
    // We just reuse your existing classes!
}